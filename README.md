# PDF-Field-Labeler
Ferramenta em Python para mapeamento e renomeação precisa de campos AcroForm em PDFs. Utiliza coordenadas (X, Y) e extração via JSON para substituir IDs genéricos por nomes padronizados, facilitando a automação de preenchimento de dados.
